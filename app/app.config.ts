export default defineAppConfig({
  ui: {
    button: {
      slots: {
        base: 'cursor-pointer',
      },
    },
    input: {
      defaultVariants: {
        color: 'neutral',
        variant: 'soft',
      },
    },
  },
});
